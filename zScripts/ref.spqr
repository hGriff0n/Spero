##
Defines a reference type, Ref[T], that can possibly be used ala C++'s `&`
##

def Ref[T+] = (mut T) T {
    # How do I get the transformations to "stick"
}

let add2 = (Int) -> .+=(2)

def main = () {
    let ref = Ref(3)
    add2(ref)

    ("{}", ref).println
}